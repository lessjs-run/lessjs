name: Test

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_call:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run type check
        run: deno check packages/core/src/index.ts packages/rpc/src/index.ts packages/ui/src/index.ts packages/adapter-lit/src/index.ts packages/adapter-lit/src/ssr.ts packages/signals/src/index.ts packages/create/cli.ts packages/app/src/index.ts packages/i18n/src/index.ts packages/content/src/index.ts

  test-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run core tests
        run: deno test packages/core/__tests__/ --allow-read --allow-write --allow-env --allow-run --allow-ffi

  test-rpc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run rpc tests
        run: deno test packages/rpc/__tests__/ --allow-read --allow-write

  test-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run ui tests
        run: deno test packages/ui/__tests__/ --allow-read --allow-write --allow-env

  test-create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run create tests
        run: deno test packages/create/__tests__/ --allow-read --allow-write --allow-env --allow-run --allow-ffi

  test-i18n:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run i18n tests
        run: deno test packages/i18n/__tests__/ --allow-read --allow-write --allow-env

  test-content:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run content tests
        run: deno test packages/content/__tests__/ --allow-read --allow-write --allow-env

  test-adapter-lit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Run adapter-lit tests
        run: deno test packages/adapter-lit/__tests__/ --allow-read --allow-write --allow-env

  build-www:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
      - name: Install dependencies
        run: deno install --node-modules-dir
      - name: Build www site
        run: deno task build:docs
