export function createRuntimeShimCode(): string {
  return "/**\n * ⚠️ AUTO-GENERATED by `deno task generate:runtime-shim` — do not edit.\n *\n * Source files:\n *   - packages/core/src/html-escape.ts\n *   - packages/core/src/render-dsd.ts\n *   - packages/core/src/render-nested.ts\n *\n * To regenerate: deno task generate:runtime-shim\n * To verify: deno task generate:runtime-shim && git diff --exit-code packages/core/src/runtime-shim.ts\n */\n\nexport { Hono } from 'hono';\nimport * as parse5 from 'parse5';\n\n// ─── Adapter Protocol ──────────────────────────────────────────\nvar _adapter = undefined;\n\nexport function registerAdapter(adapter) {\n  _adapter = adapter;\n}\n\nfunction getAdapter() {\n  return _adapter;\n}\n\nfunction isLitTemplateResultHeuristic(value) {\n  return typeof value === 'object' && value !== null && '_$litType$' in value;\n}\n\n// ─── Shim Logger ───────────────────────────────────────────────\n// Minimal logger stub — maps structured log calls to console with [LessJS] prefix.\n// Source files use createLogger('core') which adds the prefix at runtime;\n// the shim reproduces this with a static stub.\nvar log = {\n  error: function() { var a = ['[LessJS]']; a.push.apply(a, arguments); console.error.apply(console, a); },\n  warn: function() { var a = ['[LessJS]']; a.push.apply(a, arguments); console.warn.apply(console, a); },\n  debug: function() { var a = ['[LessJS]']; a.push.apply(a, arguments); console.debug.apply(console, a); },\n  info: function() { var a = ['[LessJS]']; a.push.apply(a, arguments); console.log.apply(console, a); },\n};\n\nexport { log };\n\nfunction escapeHtml(str) {\n    if (typeof str !== 'string')\n        return '';\n    if (str.__safeHtml !== undefined)\n        return str;\n    if (str.__unsafeHtml !== undefined)\n        return str;\n    return str\n        .replace(/&/g, '&amp;')\n        .replace(/</g, '&lt;')\n        .replace(/>/g, '&gt;')\n        .replace(/\"/g, '&quot;')\n        .replace(/'/g, '&#39;');\n}\n\nfunction escapeAttr(value) {\n    return value\n        .replace(/&/g, '&amp;')\n        .replace(/\"/g, '&quot;')\n        .replace(/'/g, '&#39;')\n        .replace(/</g, '&lt;')\n        .replace(/>/g, '&gt;');\n}\n\nfunction escapeAttrValue(value) {\n    if (value === null || value === undefined)\n        return '';\n    return escapeAttr(String(value));\n}\n\nfunction wrapInDocument(html, options = {}) {\n    const { title = 'LessJS', lang = 'en', clientScript = '', meta, devMode = false, routeModulePath, headExtras = '', cspNonce, } = options;\n    const nonceAttr = cspNonce ? ` nonce=\"${cspNonce}\"` : '';\n    // Security: warn if headExtras contains <script> tags, which may indicate\n    // user-supplied content being injected unsafely. Legitimate use cases exist\n    // (e.g. analytics scripts), but developers should be aware of the risk.\n    if (headExtras && /<script[\\s>]/i.test(headExtras)) {\n        log.warn('headExtras contains <script> tags. Ensure this content is developer-controlled, ' +\n            'not user-supplied, to prevent XSS. For safe URL injection, use inject.scripts instead.');\n    }\n    const metaTags = [];\n    if (meta?.description) {\n        const safeDesc = meta.description\n            .replace(/&/g, '&amp;')\n            .replace(/\"/g, '&quot;')\n            .replace(/</g, '&lt;')\n            .replace(/>/g, '&gt;');\n        metaTags.push(`  <meta name=\"description\" content=\"${safeDesc}\">`);\n    }\n    const metaBlock = metaTags.length > 0 ? '\\n' + metaTags.join('\\n') + '\\n' : '';\n    const devScripts = devMode\n        ? `\n  <script type=\"module\" src=\"/@vite/client\"${nonceAttr}></script>\n  ${routeModulePath\n            ? `<script type=\"module\"${nonceAttr}>\n  import '${routeModulePath}';\n</script>`\n            : ''}`\n        : '';\n    const safeTitle = escapeHtml(title);\n    const safeLang = escapeAttr(lang);\n    return `<!DOCTYPE html>\n<html lang=\"${safeLang}\">\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n  <title>${safeTitle}</title>${metaBlock}\n  ${headExtras}\n</head>\n<body>\n  ${html}\n  ${clientScript}${devScripts}\n</body>\n</html>`;\n}\n\nfunction serializeAttributes(props) {\n    const parts = [];\n    for (const [key, val] of Object.entries(props)) {\n        if (val === false || val === null || val === undefined)\n            continue;\n        // Convert camelCase to kebab-case so Lit's attribute observer\n        // can read the value back on client-side upgrade.\n        // e.g. currentPath → current-path\n        const attrKey = camelToKebab(key);\n        if (val === true) {\n            parts.push(attrKey);\n        }\n        else if (typeof val === 'object') {\n            // Array or Object: JSON-encode and escape for safe HTML attribute embedding.\n            // Client-side Lit deserializes via property setter (not attribute), so the\n            // JSON string only needs to survive HTML parsing, not be human-readable.\n            parts.push(`${attrKey}=\"${escapeAttrValue(JSON.stringify(val))}\"`);\n        }\n        else {\n            parts.push(`${attrKey}=\"${escapeAttrValue(val)}\"`);\n        }\n    }\n    return parts.length > 0 ? ' ' + parts.join(' ') : '';\n}\n\nfunction buildDsdTemplateAttrs(options) {\n    if (!options)\n        return '';\n    const parts = [];\n    if (options.delegatesFocus)\n        parts.push(' shadowrootdelegatesfocus');\n    if (options.serializable)\n        parts.push(' shadowrootserializable');\n    if (options.slotAssignment === 'manual')\n        parts.push(' shadowrootslotassignment=\"manual\"');\n    if (options.customElementRegistry) {\n        parts.push(` shadowrootcustomelementregistry=\"${escapeAttr(options.customElementRegistry)}\"`);\n    }\n    return parts.join('');\n}\n\nasync function renderDSD(tagName, componentClass, props = {}, sourceInfo, dsdOptions) {\n    const sourceStr = sourceInfo\n        ? `${sourceInfo.route ? ` route=\"${sourceInfo.route}\"` : ''}${sourceInfo.source ? ` source=\"${sourceInfo.source}\"` : ''}`\n        : '';\n    // 1. Instantiate the component\n    let instance;\n    try {\n        instance = new componentClass();\n    }\n    catch (err) {\n        const errMsg = err instanceof Error ? err.message : String(err);\n        log.error(`Failed to instantiate <${tagName}>:`, errMsg);\n        return (`<${tagName}${sourceStr}><!-- LessJS ERROR: Failed to instantiate <${tagName}>: ${escapeHtml(errMsg)} -->` +\n            (sourceInfo?.route ? `\\n<!-- Route: ${escapeHtml(sourceInfo.route)} -->` : '') +\n            (sourceInfo?.source ? `\\n<!-- Source: ${escapeHtml(sourceInfo.source)} -->` : '') +\n            `</${tagName}>`);\n    }\n    // 2. Set attributes/properties\n    for (const [key, value] of Object.entries(props)) {\n        try {\n            instance[key] = value;\n        }\n        catch (e) {\n            // Some properties may be read-only — safe to skip, but log for debuggability\n            log.debug(`Cannot set read-only property \"${key}\" on <${tagName}>: ${e instanceof Error ? e.message : String(e)}`);\n        }\n    }\n    // 3. DO NOT call connectedCallback in SSR.\n    // 4. Call render() to get Shadow DOM content\n    let content;\n    try {\n        const result = instance.render();\n        if (result == null) {\n            content = '';\n        }\n        else if (typeof result === 'string') {\n            content = result;\n        }\n        else {\n            const adapter = getAdapter();\n            if (adapter?.isTemplate && adapter?.render && adapter.isTemplate(result)) {\n                content = await adapter.render(result, tagName);\n            }\n            else {\n                const errDetail = isLitTemplateResultHeuristic(result)\n                    ? 'This looks like a Lit TemplateResult — install @lessjs/adapter-lit to handle it.'\n                    : `Components must return a string from render(), got ${typeof result}.`;\n                log.error(`<${tagName}> render() returned ${typeof result} instead of string. ${errDetail}`);\n                content =\n                    `<!-- LessJS ERROR: <${tagName}> render() returned ${typeof result}, expected string. ${errDetail} -->`;\n            }\n        }\n    }\n    catch (err) {\n        const errMsg = err instanceof Error ? err.message : String(err);\n        const errStack = err instanceof Error ? err.stack : '';\n        log.error(`<${tagName}> render() failed: ${errMsg}${errStack ? `\\n${errStack}` : ''}`);\n        content = `<!-- LessJS ERROR: <${tagName}> render() threw: ${escapeHtml(errMsg)} -->\\n` +\n            (errStack\n                ? `<!-- Stack: ${escapeHtml(errStack.split('\\n').slice(0, 3).join(' | '))} -->\\n`\n                : '') +\n            '<!-- Check console for full error details -->';\n    }\n    // v0.6: L2 Nested DSD — recursively render nested Custom Elements\n    content = await renderNestedCustomElements(content);\n    // 5. Extract static styles from component class\n    let styleCss = '';\n    const adapter = getAdapter();\n    if (adapter?.extractStyles) {\n        try {\n            styleCss = adapter.extractStyles(componentClass) || '';\n        }\n        catch (e) {\n            // Style extraction failed — continue without styles, but log for debuggability\n            log.debug(`extractStyles failed for <${tagName}>: ${e instanceof Error ? e.message : String(e)}`);\n        }\n    }\n    // 6. Wrap in DSD (or skip for Pure Island layer)\n    // v0.6.2: Layer 3 (pure-island) components skip DSD entirely.\n    // The framework fully owns the shadow root — no pre-rendered template.\n    const resolvedLayer = dsdOptions?.layer || instance.layer || 'dsd-static';\n    if (resolvedLayer === 'pure-island') {\n        // Pure Island: no DSD template, framework will create shadow root on client\n        const attrs = serializeAttributes(props);\n        const ssrPropsAttr = Object.keys(props).length > 0\n            ? ` data-ssr-props=\"${escapeAttrValue(JSON.stringify(props))}\"`\n            : '';\n        return `<${tagName}${attrs}${ssrPropsAttr}${sourceStr}></${tagName}>`;\n    }\n    // Layer 1 (dsd-static) and Layer 2 (dsd-interactive): emit DSD template\n    const attrs = serializeAttributes(props);\n    const ssrPropsAttr = Object.keys(props).length > 0\n        ? ` data-ssr-props=\"${escapeAttrValue(JSON.stringify(props))}\"`\n        : '';\n    const styleTag = styleCss ? `\\n    <style>${styleCss}</style>` : '';\n    // Build DSD template attributes per HTML Living Standard\n    const dsdAttrs = buildDsdTemplateAttrs(dsdOptions);\n    return `<${tagName}${attrs}${ssrPropsAttr}${sourceStr}>\n  <template shadowrootmode=\"open\"${dsdAttrs}>${styleTag}\n    ${content}\n  </template>\n</${tagName}>`;\n}\n\nasync function renderDSDByName(tagName, props = {}, sourceInfo, dsdOptions) {\n    const cls = globalThis.customElements?.get(tagName);\n    if (!cls) {\n        log.warn(`<${tagName}> is not registered — rendering as void element`);\n        const attrs = serializeAttributes(props);\n        return `<${tagName}${attrs}></${tagName}>`;\n    }\n    return await renderDSD(tagName, cls, props, sourceInfo, dsdOptions);\n}\n\nfunction kebabToCamel(str) {\n    return str.replace(/-([a-z])/g, (_, c) => c.toUpperCase());\n}\n\nfunction parseAttrsToProps(attrs) {\n    const props = {};\n    for (const attr of attrs) {\n        const key = attr.name;\n        const value = attr.value;\n        if (value === '') {\n            // Boolean attribute (no value): e.g. disabled, home\n            props[key] = true;\n        }\n        else if (key.startsWith('data-')) {\n            // data-* attributes: preserve as-is (don't camelCase)\n            props[key] = value;\n        }\n        else {\n            const camelKey = kebabToCamel(key);\n            // Try to parse as JSON for array/object values from SSR property bindings.\n            // The Lit SSR adapter converts .navItems=\"${arr}\" → nav-items=\"[{...}]\"\n            // so we need to parse the JSON back to a JS value for renderDSD().\n            if (value.startsWith('[') || value.startsWith('{')) {\n                try {\n                    const parsed = JSON.parse(value);\n                    if (typeof parsed === 'object' && parsed !== null) {\n                        props[camelKey] = parsed;\n                        continue;\n                    }\n                }\n                catch {\n                    // Not valid JSON — treat as string\n                }\n            }\n            props[camelKey] = value;\n        }\n    }\n    return props;\n}\n\nfunction elementAlreadyHasDSD(node) {\n    for (const child of node.childNodes) {\n        if (child.nodeName === 'template') {\n            const template = child;\n            const shadowAttr = template.attrs.find((a) => a.name === 'shadowrootmode' && a.value === 'open');\n            if (shadowAttr)\n                return true;\n        }\n        // Skip text nodes (whitespace) — only check the first element child\n        if (child.nodeName !== '#text')\n            break;\n    }\n    return false;\n}\n\nfunction isCustomElementName(tagName) {\n    return /^[a-z][a-z0-9]*-[a-z0-9-]+$/i.test(tagName);\n}\n\nfunction inferDsdOptions(_tagName, cls) {\n    const opts = {};\n    const proto = cls;\n    if (proto.delegatesFocus === true)\n        opts.delegatesFocus = true;\n    if (proto.serializable === true)\n        opts.serializable = true;\n    if (proto.slotAssignment === 'manual')\n        opts.slotAssignment = 'manual';\n    if (typeof proto.customElementRegistry === 'string') {\n        opts.customElementRegistry = proto.customElementRegistry;\n    }\n    return opts;\n}\n\nfunction isInsideDsdTemplate(node) {\n    let parent = node.parentNode;\n    while (parent) {\n        if ('nodeName' in parent && parent.nodeName === 'template') {\n            const tpl = parent;\n            if (tpl.attrs.some((a) => a.name === 'shadowrootmode' && a.value === 'open')) {\n                return true;\n            }\n        }\n        parent = parent.parentNode;\n    }\n    return false;\n}\n\nasync function renderNestedCustomElements(html) {\n    if (!globalThis.customElements?.get)\n        return html;\n    // Import renderDSD dynamically to avoid circular dependency\n    // renderDSD is defined in this file\n    // Use parseFragment() — NOT parse(). parse5.parse() wraps fragments in\n    // <html><head><body>, which would appear inside shadow DOM when this\n    // function processes render() output (which is a fragment, not a document).\n    const ast = parse5.parseFragment(html);\n    // Collect custom element nodes in bottom-up (deepest-first) order\n    const ceNodes = [];\n    function collectCustomElements(node) {\n        if (!('tagName' in node))\n            return;\n        const element = node;\n        // Recurse into children first (bottom-up: children before parent)\n        if (element.childNodes) {\n            for (const child of element.childNodes) {\n                collectCustomElements(child);\n            }\n        }\n        // Check if this is a custom element\n        const tagName = element.tagName;\n        if (!tagName || !isCustomElementName(tagName))\n            return;\n        // Skip if inside a DSD template (shadow DOM content)\n        if (isInsideDsdTemplate(element))\n            return;\n        // Check if registered\n        if (!globalThis.customElements.get(tagName))\n            return;\n        // Skip if already has DSD\n        if (elementAlreadyHasDSD(element))\n            return;\n        ceNodes.push(element);\n    }\n    // Fragment childNodes are the top-level nodes directly\n    for (const child of ast.childNodes ?? []) {\n        collectCustomElements(child);\n    }\n    // Process each custom element (already in bottom-up order)\n    for (const ceNode of ceNodes) {\n        const tagName = ceNode.tagName;\n        const Cls = globalThis.customElements.get(tagName);\n        if (!Cls)\n            continue;\n        const props = parseAttrsToProps(ceNode.attrs);\n        const dsdOpts = inferDsdOptions(tagName, Cls);\n        // Render DSD HTML for this component\n        const dsdHtml = await renderDSD(tagName, Cls, props, undefined, dsdOpts);\n        // Parse the DSD HTML into a fragment\n        const dsdFragment = parse5.parseFragment(dsdHtml);\n        // The DSD fragment contains a top-level CE element (e.g. <less-layout>).\n        // We must NOT insert this entire element — that would create double nesting\n        // (<existing-ce><new-ce-from-dsd>...</new-ce></existing-ce>).\n        // Instead, extract the inner children (the <template> and any light DOM)\n        // and merge new attributes (like data-ssr-props) onto the existing CE node.\n        const dsdCeElement = dsdFragment.childNodes?.find((child) => 'tagName' in child && child.tagName === tagName);\n        // Merge attributes from DSD CE onto the existing CE node\n        // (e.g. data-ssr-props, source, route attrs added by renderDSD)\n        if (dsdCeElement) {\n            for (const attr of dsdCeElement.attrs) {\n                // Only add attrs that don't already exist on the CE node\n                const alreadyExists = ceNode.attrs.some((a) => a.name === attr.name);\n                if (!alreadyExists) {\n                    ceNode.attrs.push(attr);\n                }\n            }\n        }\n        // Collect light DOM children (slot content) — everything currently inside the CE\n        const lightDomChildren = [...ceNode.childNodes];\n        // Clear the CE node and repopulate with DSD content + light DOM\n        ceNode.childNodes = [];\n        // Insert only the CHILDREN of the DSD CE element (not the CE wrapper itself)\n        // This is the <template shadowrootmode=\"open\"> and any other shadow DOM content\n        const dsdChildren = dsdCeElement?.childNodes ?? dsdFragment.childNodes ?? [];\n        for (const child of dsdChildren) {\n            child.parentNode = ceNode;\n            ceNode.childNodes.push(child);\n        }\n        // Append light DOM children after the DSD template (for slot projection)\n        // Only add non-empty text nodes and element nodes\n        for (const child of lightDomChildren) {\n            // Skip pure whitespace text nodes between the DSD template and light DOM\n            if (child.nodeName === '#text' &&\n                child.value.trim() === '') {\n                continue;\n            }\n            child.parentNode = ceNode;\n            ceNode.childNodes.push(child);\n        }\n    }\n    // Serialize fragment back to HTML (no <html><head><body> wrapper)\n    return parse5.serialize(ast);\n}";
}
