{
  "name": "@kissjs/rpc",
  "version": "0.2.5",
  "description": "KISS RPC — framework-agnostic fetch abstraction with retry and cancellation",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": { "types": "./src/index.ts", "import": "./src/index.ts" } },
  "files": ["src"],
  "scripts": {
    "build": "vite build --config vite.config.build.ts",
    "dev": "vite build --config vite.config.build.ts --watch"
  },
  "devDependencies": { "vite-plugin-dts": "^4.5.0" }
}
