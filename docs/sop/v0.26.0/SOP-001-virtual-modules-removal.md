# SOP-001: Virtual Modules 移除 — 框架数据层去耦合

> **Priority**: P0 | **预估**: 8h | **依赖**: 无 | **ADR**: ADR-0061

## Objective

移除 3 个 Vite 虚拟模块，改用 @openelement/content 和 @openelement/i18n 子路径导出。

## Background

- 55 个路由 `import from 'virtual:less-nav'` — Vite 插件注入数据
- 5 个路由 `import from 'virtual:less-blog-data'`
- entry-renderer 生成代码硬编码 virtual: 导入
- Deno import() 无法解析 → tagName 检测失败
- IDE 无法跳转/类型推断

## Step 1: 创建 Nav 数据导出 (1h)

**文件**: `packages/content/src/nav/writer.ts`（新建）

```ts
export function writeNavModule(data: NavData): string {
  return [
    '// Auto-generated by @openelement/content',
    `export const headerNav = ${JSON.stringify(data.headerNav)};`,
    `export const navSections = ${JSON.stringify(data.navSections)};`,
  ].join('\n');
}
```

**文件**: `packages/content/deno.json`

```json
"./nav-data": "./src/nav/writer.ts"
```

## Step 2: 创建 Blog/I18n 数据导出 (1h)

**文件**: `packages/content/src/blog/blog-data.ts`

```ts
export function writeBlogDataModule(posts: BlogPost[]): string { ... }
```

**文件**: `packages/i18n/src/i18n-data.ts`

```ts
export function writeI18nDataModule(locales, defaultLocale): string { ... }
```

deno.json 新增 `"./blog-data"` 和 `"./data"` exports。

## Step 3: 批量替换 55+5 路由文件 (2h)

```bash
# Nav (55 files)
find www/app/routes -name '*.ts' -o -name '*.tsx' | \
  xargs sed -i "s|from 'virtual:less-nav'|from '@openelement/content/nav'|g"

# Blog (5 files)
sed -i "s|from 'virtual:less-blog-data'|from '@openelement/content/blog-data'|g" \
  www/app/routes/blog/[slug].tsx \
  www/app/routes/blog/index.ts \
  www/app/routes/decisions/[slug].ts \
  www/app/routes/decisions/index.ts
```

验证: `grep -r "virtual:less-nav\|virtual:less-blog" www/app/routes/` → 零结果

## Step 4: Vite 插件改为写入文件 (2h)

**文件**: `packages/adapter-vite/src/phase-context.ts`

移除 `virtual:less-blog-data` 和 `virtual:less-i18n-data` 的 resolveId/load handler。
在 buildStart 时写入 `app/data/_generated-nav.ts`、`_generated-blog-data.ts`、`_generated-i18n-data.ts`。

## Step 5: 添加 import map (0.5h)

**文件**: `www/deno.json`

```json
{
  "imports": {
    "@openelement/content/nav": "./app/data/_generated-nav.ts",
    "@openelement/content/blog-data": "./app/data/_generated-blog-data.ts",
    "@openelement/i18n/data": "./app/data/_generated-i18n-data.ts"
  }
}
```

Vite plugin 同步配置 resolve.alias。

## Step 6: 全量回归 (1.5h)

```bash
deno fmt && deno lint && deno task typecheck
deno test --allow-read --allow-write --allow-env --allow-net --allow-run
deno task build:docs
deno test packages/adapter-vite/__tests__/ssg-smoke.test.ts
```

**验收**: 零 virtual: 在路由文件中，所有 gate 绿色，SSG DSD 输出正常。
