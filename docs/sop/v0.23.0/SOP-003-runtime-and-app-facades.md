# SOP-003: Runtime and App Facades

> Version: v0.23.0\
> Priority: P0\
> Status: IMPLEMENTED\
> Depends on: SOP-001, SOP-002

## Objective

Separate two different user-facing conveniences:

- component authoring imports;
- application configuration imports.

## Current Problem

`@openelement/app` correctly owns `lessjs()` configuration composition, but
component authoring convenience still tends to fall back to `@openelement/core`.
That pulls `core` toward a batteries-included facade instead of a runtime
kernel.

## Target Contract

Use separate facades:

```text
@openelement/runtime
  authoring facade for DsdElement, html, StyleSheet, signal, computed, effect

@openelement/app
  configuration facade for lessjs(), content, i18n, and adapter-vite assembly

@openelement/core
  runtime kernel, not the product facade
```

The exact package name can change, but the responsibilities must stay separate.

## Facade Decision Criteria

Add `@openelement/runtime` only if it improves all three:

- generated templates can use one authoring import without bloating `core`;
- `core` can stop re-exporting implementation packages as the recommended path;
- the new package does not introduce a publish cycle.

Reject `@openelement/runtime` if it is only a branding alias for `@openelement/core`.

## Procedure

### Step 1: Decide Whether to Add `@openelement/runtime`

- [ ] Audit generated templates, docs, UI components, and examples for
      component authoring imports.
- [ ] Decide whether `@openelement/runtime` is worth the extra package.
- [ ] If rejected, document the chosen alternative and keep `core` small.

Acceptance:

- [ ] The decision is recorded in the v0.23 implementation notes.
- [ ] The authoring import path is consistent across docs and templates.
- [ ] The decision explains how `packages/create/cli.ts` examples should import
      `DsdElement`, `html`, `StyleSheet`, `signal`, `computed`, and `effect`.

### Step 2: Define Runtime Facade Exports

- [ ] Export `DsdElement`, `html`, `unsafeHTML`, and template types.
- [ ] Export `StyleSheet` from its canonical owner.
- [ ] Export signal helpers from `@openelement/signals`.
- [ ] Avoid exporting Vite, content, i18n, or build contracts.

Acceptance:

- [ ] The facade has no Vite dependency.
- [ ] The facade does not create a publish cycle.
- [ ] The facade depends only on runtime owners such as `core`, `signals`, and
      `style-sheet`.

### Step 3: Keep `@openelement/app` Configuration-Focused

- [ ] Keep `lessjs()` as the recommended Vite config entry.
- [ ] Keep `content` and `i18n` composition in `@openelement/app`.
- [ ] Do not move runtime component APIs into `@openelement/app`.
- [ ] Ensure `@openelement/app` imports contracts from the contracts package, not
      adapter internals.

Acceptance:

- [ ] `vite.config.ts` generated by `@openelement/create` remains small.
- [ ] `@openelement/app` remains the only recommended config facade.
- [ ] `@openelement/app` does not become the place users import component authoring
      APIs from.

### Step 4: Migrate Templates and Docs

- [ ] Update `@openelement/create` templates.
- [ ] Update README quickstart examples.
- [ ] Update package READMEs where imports change.
- [ ] Keep compatibility examples for one minor release if needed.

Acceptance:

- [ ] Fresh generated projects build locally and from JSR.
- [ ] Public docs do not recommend importing implementation packages for
      ordinary app authoring unless that is deliberate.
- [ ] The generated project import map declares every direct import used by the
      generated source.

## Verification

```sh
deno test packages/create/__tests__/cli.test.ts --allow-read --allow-write --allow-env --allow-run --allow-ffi
deno task typecheck
deno task build
deno run -A packages/create/cli.ts test-blog
```

## Exit Criteria

- App configuration and component authoring are separate concepts.
- `@openelement/core` is not forced to be the DX barrel.
- Generated templates teach the intended architecture.

## v0.23.0 Result

- `@openelement/runtime` is accepted as the component-authoring facade for
  `DsdElement`, templates, `StyleSheet`, and signal helpers.
- `@openelement/app` remains the Vite/configuration facade for `lessjs()`,
  content, i18n, and adapter assembly.
- `@openelement/create` generated routes and islands import authoring APIs from
  `@openelement/runtime`, and generated `deno.json` declares that direct import.
- `@openelement/app` no longer imports `@openelement/adapter-vite/build-context`.
