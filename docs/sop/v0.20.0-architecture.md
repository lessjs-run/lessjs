# v0.20.0 鏋舵瀯璁捐 鈥?Ocean-Island 鏋舵瀯

> 鈿狅笍 **SUPERSEDED**: This document has been merged into [ADR-0036](../adr/0036-ocean-island-architecture.md). See `docs/sop/v0.20.0/` for detailed SOPs.

---

# v0.20.0 鏋舵瀯璁捐 鈥?Ocean-Island 鏋舵瀯 (ARCHIVED)

> **浣滆€?*: 楂樿杩滐紙Gao锛壜?鏋舵瀯甯?> **鏃ユ湡**: 2026-05-20
> **鐗堟湰**: v1.0
> **鍩轰簬**: [v0.20.0 PRD](./v0.20.0-prd.md)

---

## 1. 鏋舵瀯姒傝

```
v0.19.0锛堝綋鍓嶏級                           v0.20.0锛堢洰鏍囷級
鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€           鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
@openelement/adapter-lit                      @openelement/core
鈹溾攢鈹€ DsdLitElement (Lit 缁戝畾)      鈫?    鈹溾攢鈹€ DsdElement (闆舵鏋?
鈹溾攢鈹€ WithDsdHydration (Mixin)             鈹溾攢鈹€ renderDsd() (宸叉湁锛屽吋瀹?
鈹溾攢鈹€ ssr.ts (Lit鈫扝TML 鎻掑€?               鈹溾攢鈹€ hydrateEvents 鍗忚
鈹?                                       鈹斺攢鈹€ CSSStyleSheet 鎻愬彇鍣?@openelement/ui                               @openelement/adapter-lit
鈹溾攢鈹€ less-button (Lit)              鈫?    鈹斺攢鈹€ 浠呮湇鍔?Pure Island SSR
鈹溾攢鈹€ less-card (Lit)                      (淇濈暀涓嶅彉锛屼笉鍐嶆墿澶?
鈹溾攢鈹€ ... (10 缁勪欢鍏?Lit)
鈹斺攢鈹€ design-tokens (Lit CSSResult)        @openelement/ui
                                         鈹溾攢鈹€ less-button (鍘熺敓 DsdElement)
                                         鈹溾攢鈹€ less-card (鍘熺敓 DsdElement)
                                         鈹溾攢鈹€ ... (9 缁勪欢鍘熺敓)
                                         鈹溾攢鈹€ less-hero-ping (淇濈暀 Lit)
                                         鈹斺攢鈹€ design-tokens (CSSStyleSheet)
```

---

## 2. DsdElement 鍩虹被璁捐

### 2.1 婧愮爜浣嶇疆

```
packages/core/src/dsd-element.ts    # 鏂板
packages/core/src/index.ts          # 鏇存柊瀵煎嚭
```

### 2.2 API 濂戠害

```typescript
// packages/core/src/dsd-element.ts

import type { HydrateEventDescriptor } from './types.js';

/**
 * Base class for DSD (Declarative Shadow DOM) components.
 *
 * Zero framework dependency. Pure HTMLElement subclass.
 *
 * SSR contract:
 *   render(): string  鈫? returns Shadow DOM HTML
 *   鈫?renderDsd() wraps in <template shadowrootmode="open">
 *
 * Client contract:
 *   1. Browser parses DSD 鈫?Shadow DOM auto-attached
 *   2. customElements.define() upgrades element
 *   3. createRenderRoot() detects existing shadow root 鈫?_dsdHydrated = true
 *   4. connectedCallback() auto-binds hydrateEvents
 *   5. disconnectedCallback() cleanup via AbortController
 *
 * Components extend this class MUST implement:
 *   - render(): string
 *
 * Components MAY declare:
 *   - static hydrateEvents: HydrateEventDescriptor[]
 *   - static styles: CSSStyleSheet | CSSStyleSheet[]
 *   - static observedAttributes: string[]
 *   - static delegatesFocus: boolean
 *   - static formAssociated: boolean
 *
 * Design rationale:
 *   - render() returns string (not TemplateResult) 鈫?no Lit dependency
 *   - CSSStyleSheet (not CSSResult) 鈫?native platform API
 *   - hydrateEvents stays 鈫?proven pattern, works well
 *   - attributeChangedCallback 鈫?replaces Lit's updated()
 *   - :state() via ElementInternals 鈫?CSS custom states
 */
export class DsdElement extends HTMLElement {
  /** Declarative event bindings for DSD hydration */
  static hydrateEvents?: HydrateEventDescriptor[];

  /** CSSStyleSheet(s) for adoptedStyleSheets */
  static styles?: CSSStyleSheet | CSSStyleSheet[];

  /** Whether DSD has pre-populated the shadow root */
  protected _dsdHydrated = false;

  /** AbortController for event listener lifecycle */
  private _hydrateAbortController?: AbortController;

  /** ElementInternals for :state() pseudo-classes */
  protected _internals?: ElementInternals;

  // 鈹€鈹€ Shadow Root 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€

  /**
   * Reuse existing shadow root when DSD-pre-populated,
   * otherwise create new one.
   */
  protected createRenderRoot(): ShadowRoot {
    if (this.shadowRoot && this.shadowRoot.childElementCount > 0) {
      this._dsdHydrated = true;
      return this.shadowRoot;
    }
    const root = this.attachShadow({ mode: 'open' });
    // Apply styles
    if (this._styles) {
      root.adoptedStyleSheets = [this._styles];
    }
    return root;
  }

  // 鈹€鈹€ Styles 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€

  private get _styles(): CSSStyleSheet | undefined {
    const ctor = this.constructor as typeof DsdElement;
    if (!ctor.styles) return undefined;
    if (Array.isArray(ctor.styles)) {
      // Merge all sheets into one
      const merged = new CSSStyleSheet();
      merged.replaceSync(
        ctor.styles.map((s) => [...s.cssRules].map((r) => r.cssText).join('\n')).join('\n'),
      );
      return merged;
    }
    return ctor.styles;
  }

  // 鈹€鈹€ Lifecycle 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€

  connectedCallback(): void {
    // Ensure shadow root exists (CSR fallback)
    if (!this.shadowRoot) {
      this.createRenderRoot();
    }

    // CSR-only: populate shadow DOM from render() if not DSD-hydrated
    if (!this._dsdHydrated && this.shadowRoot) {
      this.shadowRoot.innerHTML = this.render();
    }

    // DSD: bind events
    if (this._dsdHydrated) {
      this._hydrateEvents();
    }

    // Form-associated
    if ((this.constructor as typeof DsdElement).formAssociated) {
      this._internals = this.attachInternals();
    }
  }

  disconnectedCallback(): void {
    this._hydrateAbortController?.abort();
    this._hydrateAbortController = undefined;
  }

  /**
   * Called when observed attributes change.
   * Override to sync DOM after attribute changes.
   */
  attributeChangedCallback(name: string, oldValue: string | null, newValue: string | null): void {
    if (oldValue === newValue) return;
    // Subclasses override to sync DOM
  }

  // 鈹€鈹€ SSR Contract 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€

  /**
   * Return Shadow DOM HTML string for SSR.
   * MUST be overridden by subclasses.
   */
  render(): string {
    return '';
  }

  // 鈹€鈹€ Event Hydration 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€

  /**
   * Bind declared events to existing shadow DOM elements.
   * Ported from WithDsdHydration._hydrateEvents() with same logic.
   */
  protected _hydrateEvents(): void {
    if (!this.shadowRoot) return;

    const ctor = this.constructor as typeof DsdElement;
    const events = ctor.hydrateEvents || [];
    if (events.length === 0) return;

    this._hydrateAbortController = new AbortController();
    const { signal } = this._hydrateAbortController;

    for (const desc of events) {
      if (desc.method.startsWith('__')) continue; // M-17: prototype pollution guard
      const elements = this.shadowRoot.querySelectorAll(desc.selector);
      for (const el of elements) {
        const handler = (this as unknown as Record<string, unknown>)[desc.method];
        if (typeof handler === 'function') {
          el.addEventListener(desc.event, (handler as EventListener).bind(this), { signal });
        }
      }
    }
  }
}
```

### 2.3 涓?DsdLitElement 鐨勫樊寮?

| 缁村害          | DsdLitElement                     | DsdElement                                               |
| --------------- | --------------------------------- | -------------------------------------------------------- |
| 鍩虹被          | LitElement                        | HTMLElement                                              |
| render() 杩斿洖 | TemplateResult                    | string                                                   |
| 鏍峰紡          | Lit `css` tagged template         | CSSStyleSheet                                            |
| 鍝嶅簲寮?       | `static properties` + `updated()` | `static observedAttributes` + `attributeChangedCallback` |
| DSD 妫€娴?      | `createRenderRoot()` 瑕嗙洊       | 鍚岄€昏緫锛岀Щ妞?                                        |
| hydrateEvents   | 鉁?                               | 鉁?绉绘                                                 |
| 渚濊禆          | lit (~6KB gzip)                   | 0 KB                                                     |
| delegatesFocus  | Lit 鍐呯疆                        | 鎵嬪姩 attachShadow({delegatesFocus})                    |

### 2.4 DSD 妫€娴嬩笌 Shadow Root 澶嶇敤

```
鍏抽敭閫昏緫锛堢Щ妞嶈嚜 WithDsdHydration.createRenderRoot锛夛細

if (this.shadowRoot && this.shadowRoot.childElementCount > 0) {
  // DSD 宸叉覆鏌擄細澶嶇敤宸叉湁 shadow root
  this._dsdHydrated = true;
  return this.shadowRoot;
}
// CSR 鍦烘櫙锛氬垱寤烘柊 shadow root
const root = this.attachShadow({ mode: 'open', delegatesFocus });
root.adoptedStyleSheets = [this._styles];
return root;
```

## 娉ㄦ剰锛歚attachShadow`闇€瑕佸湪`createRenderRoot`涓喅瀹?`delegatesFocus`銆乣mode` 绛夊弬鏁般€傚洜姝?DsdElement 鐨勬瀯閫犲嚱鏁颁笉 attachShadow锛岃€屾槸鐢?`createRenderRoot()` 鍦?`connectedCallback` 鏃舵噿鍒濆鍖栥€?

## 3. SSR 绠＄嚎閫傞厤

### 3.1 鐜扮姸

`renderDsd()` (packages/core/src/render-dsd.ts) **宸茬粡鏀寔 `render(): string`**锛?

```typescript
// 绗?177-181 琛岋細
const result = instance.render();
if (typeof result === 'string') {
  content = result; // 鈫?DsdElement 鐩存帴璧拌繖鏉¤矾
}
```

### 3.2 DsdElement 鏍峰紡鎻愬彇

褰撳墠鏍峰紡鎻愬彇渚濊禆閫傞厤鍣紙绗?250-269 琛岋級銆傚浜?DsdElement锛岄渶瑕佹柊澧炲師鐢熸彁鍙栬矾寰勶細

```typescript
// render-dsd.ts 鏂板閫昏緫锛?
// 5. Extract styles
let styleCss = '';

// 5a. Try native DsdElement styles (CSSStyleSheet)
const ctor = componentClass as unknown as { styles?: CSSStyleSheet | CSSStyleSheet[] };
if (ctor.styles) {
  const sheets = Array.isArray(ctor.styles) ? ctor.styles : [ctor.styles];
  for (const sheet of sheets) {
    try {
      for (const rule of [...sheet.cssRules]) {
        styleCss += rule.cssText + '\n';
      }
    } catch (e) {
      // Cross-origin sheet, skip
      log.debug(`Cannot read CSSStyleSheet for <${tagName}>`);
    }
  }
}

// 5b. Fallback: registered adapters (Lit, etc.)
if (!styleCss) {
  for (const adapter of getRegisteredAdapters()) {
    // ... 鐜版湁閫昏緫
  }
}
```

### 3.3 鍙樻洿鑼冨洿

| 鏂囦欢                            | 鍙樻洿                                            |
| --------------------------------- | ------------------------------------------------- |
| `packages/core/src/render-dsd.ts` | +15 琛岋細CSSStyleSheet 鎻愬彇                    |
| `packages/core/src/types.ts`      | 鏃犻渶鍙樻洿锛圚ydrateEventDescriptor 宸插瓨鍦級 |
| `packages/core/src/index.ts`      | +1 琛岋細瀵煎嚭 DsdElement                        |
| `packages/adapter-lit/`           | **涓嶅彉**锛堜繚鐣欏吋瀹?Pure Island锛?           |

---

## 4. 缁勪欢杩佺Щ鏂规

### 4.1 杩佺Щ妯″紡

```typescript
// 鈹€鈹€鈹€ 杩佺Щ鍓嶏紙Lit锛?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
import { css, html, nothing } from 'lit';
import { DsdLitElement } from '@openelement/adapter-lit';
import { lessDesignTokens } from './design-tokens.js';

class LessButton extends DsdLitElement {
  static override styles = [lessDesignTokens, css`...`];
  static override properties = { variant: { type: String, reflect: true } };

  override render() {
    if (this._dsdHydrated) return nothing;
    return html`<button class="btn btn--${this.variant}" ?disabled=${this.disabled}>
      <slot></slot>
    </button>`;
  }

  override updated(changed: Map<string, unknown>) {
    if (changed.has('disabled')) this._updateState();
  }
}


// 鈹€鈹€鈹€ 杩佺Щ鍚庯紙鍘熺敓 DsdElement锛?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
import { DsdElement, type HydrateEventDescriptor } from '@openelement/core';

const sheet = new CSSStyleSheet();
sheet.replaceSync(`
  ${tokenCSS}           // 鈫?鍐呰仈 token锛堟垨鐙珛 sheet锛?  :host { display: inline-block; }
  .btn { /* ... */ }
  .btn--primary { /* ... */ }
`);

class LessButton extends DsdElement {
  static observedAttributes = ['variant', 'size', 'disabled'];
  static styles = sheet;
  static delegatesFocus = true;
  static formAssociated = true;
  static hydrateEvents: HydrateEventDescriptor[] = [
    { selector: 'button', event: 'click', method: '_handleClick' },
  ];

  protected _internals?: ElementInternals;

  constructor() {
    super();
    this._internals = this.attachInternals();
  }

  connectedCallback(): void {
    super.connectedCallback();
    this._syncDOM();
  }

  attributeChangedCallback(name: string, old: string | null, val: string | null) {
    if (old === val) return;
    this._syncDOM();
  }

  render(): string {
    const v = this.getAttribute('variant') || 'default';
    const s = this.getAttribute('size') || 'md';
    const d = this.hasAttribute('disabled');
    const h = this.getAttribute('href');
    if (h) {
      return `<a class="btn btn--${v} btn--${s}"
        part="control" href="${h}" ${d ? 'aria-disabled="true"' : ''}>
        <slot></slot></a>`;
    }
    return `<button class="btn btn--${v} btn--${s}"
      part="control" ${d ? 'disabled' : ''}>
      <slot></slot></button>`;
  }

  _syncDOM() {
    const btn = this.shadowRoot?.querySelector('.btn') as HTMLElement | null;
    if (!btn) return;
    const v = this.getAttribute('variant') || 'default';
    const s = this.getAttribute('size') || 'md';
    btn.className = `btn btn--${v} btn--${s}`;
    if (btn instanceof HTMLButtonElement) {
      btn.disabled = this.hasAttribute('disabled');
    }
    this._updateState();
  }

  _handleClick() {
    this.dispatchEvent(new CustomEvent('less-click', { bubbles: true, composed: true }));
  }
}
```

### 4.2 缁勪欢杩佺Щ浼樺厛绾?

| 浼樺厛绾? | 缁勪欢              | 琛屾暟 | 澶嶆潅搴?   | 鍘熷洜                                               |
| --------- | ------------------- | ------ | ----------- | ---------------------------------------------------- |
| **P0-A**  | `less-card`         | 96     | 馃煝 浣?    | 绾睍绀猴紝鏃犱氦浜掞紝楠岃瘉 DsdElement 鏈€绠€璺緞 |
| **P0-B**  | `less-callout`      | ~60    | 馃煝 浣?    | 绾睍绀猴紝楠岃瘉 token 杩佺Щ                        |
| **P0-C**  | `less-step-card`    | ~100   | 馃煝 浣?    | 绾睍绀?                                             |
| **P0-D**  | `less-button`       | 251    | 馃煛 涓?    | 灞炴€ч┍鍔?+ :state()锛岄涓氦浜掔粍浠?              |
| **P1-A**  | `less-input`        | ~150   | 馃煛 涓?    | 灞炴€?+ 琛ㄥ崟鍏宠仈                                 |
| **P1-B**  | `less-code-block`   | ~120   | 馃煛 涓?    | 澶嶅埗浜や簰                                         |
| **P1-C**  | `less-theme-toggle` | ~80    | 馃煛 涓?    | 鐐瑰嚮浜や簰                                         |
| **P1-D**  | `less-dialog`       | ~200   | 馃煚 杈冮珮 | 鏄剧ず/闅愯棌鐘舵€?                                  |
| **P1-E**  | `less-layout`       | 1200   | 馃敶 楂?    | 鏈€澶嶆潅缁勪欢锛屽垎灞傝縼绉?                       |
| **P2-A**  | `less-search`       | ~500   | 馃敶 楂?    | overlay + SPA + 鎼滅储                               |
| **P2-B**  | `less-hero-ping`    | ~80    | N/A         | 淇濈暀 Lit锛堢函 Island锛?                           |

### 4.3 less-layout 鍒嗗眰杩佺Щ绛栫暐

`less-layout`锛?200 琛岋級鏄渶澶嶆潅鐨勭粍浠讹紝寤鸿鍒?3 姝ワ細

1. **Step 1**: 澶栬杩佺Щ锛坮ender() 鈫?string锛屾牱寮?鈫?CSSStyleSheet锛夆€?涓嶆敼閫昏緫
2. **Step 2**: 浜嬩欢杩佺Щ锛坔ydrateEvents 鏇夸唬 Lit @click锛夆€?涓嶆敼缁撴瀯
3. **Step 3**: 鐘舵€佽縼绉伙紙attributeChangedCallback 鏇夸唬 updated()锛夆€?鏈€缁堟敹鏁?

---

## 5. CSS 绠€鍖栨爤璁捐

> **v1.1 淇**: 鍘熸柟妗堬紙@property + CSS Custom Props + CSSStyleSheet + CSS Parts + :state()锛夎繃浜庡鏉傘€傜畝鍖栦负涓夊眰銆?

### 5.1 鏍稿績鍘熷垯

*_鑳界敤鏍囧噯搴撳氨涓嶇敤鑷繁鍐欍€侽pen Props 鏄璁?token 鐨勬爣鍑嗙瓟妗堚€斺€斿嚑鐧句釜鐜版垚鐨?CSS 鍙橀噺锛岄浂 JS锛岄浂鏋勫缓姝ラ銆?_

### 5.2 涓夊眰鏋舵瀯

````
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?             绠€鍖?CSS 鏍堬紙3 灞傦級               鈹?鈹溾攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                                              鈹?鈹? Layer 1: Open Props                          鈹?鈹? 鈹溾攢鈹€ 璁捐 token锛氶棿璺?棰滆壊/闃村奖/鍔ㄦ晥/瀛椾綋      鈹?鈹? 鈹溾攢鈹€ 绾?CSS 鏂囦欢锛岄浂 JS锛岄浂鏋勫缓姝ラ            鈹?鈹? 鈹溾攢鈹€ CSS Custom Properties 绌块€?Shadow DOM   鈹?鈹? 鈹溾攢鈹€ 鍐呯疆 light/dark 涓婚                      鈹?鈹? 鈹斺攢鈹€ 鏇夸唬褰撳墠鐨?color-values.ts +              鈹?鈹?      design-tokens.ts (~100 琛?鈫?0 琛?       鈹?鈹?                                              鈹?鈹? Layer 2: CSSStyleSheet + adoptedStyleSheets  鈹?鈹? 鈹溾攢鈹€ 姣忎釜缁勪欢涓€涓?sheet                        鈹?鈹? 鈹溾攢鈹€ 涓€娆?parse锛屾墍鏈夊疄渚嬪叡浜?                 鈹?鈹? 鈹溾攢鈹€ 寮曠敤 Open Props 鍙橀噺                      鈹?鈹? 鈹斺攢鈹€ 鏇夸唬 Lit css\`...\` + 鍐呰仈 cssText      鈹?鈹?                                              鈹?鈹? Layer 3: CSS Parts ::part()                  鈹?鈹? 鈹溾攢鈹€ 缁勪欢澶栭儴瀹氬埗 API                          鈹?鈹? 鈹斺攢鈹€ less-button::part(control) { ... }       鈹?鈹?                                              鈹?鈹? 鍙€夛紙涓嶅己鍒讹紝闄嶇骇鏃犲锛夛細                     鈹?鈹? 鈹溾攢鈹€ @property 鈥?鍙橀噺绫诲瀷瀹夊叏                  鈹?鈹? 鈹斺攢鈹€ :state()  鈥?鑷畾涔夌姸鎬佷吉绫?               鈹?鈹?                                              鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

### 5.3 Open Props 涓?DSD 鐨勫吋瀹规€?
CSS Custom Properties **澶╃劧绌块€?Shadow DOM 杈圭晫**銆侽pen Props 鍦?DSD 涓畬鍏ㄥ彲鐢細

```html
<template shadowrootmode="open">
  <style>
    /* SSR 鏃跺唴鑱斿彉閲忓€硷紙鏋勫缓鏃?tree-shake锛?*/
    --size-2: 8px;
    --gray-9: #212529;
    --radius-2: 8px;

    .btn {
      padding: var(--size-2) var(--size-4);
      color: var(--gray-9);
      border-radius: var(--radius-2);
    }
  </style>
  <button class="btn" part="control"><slot></slot></button>
</template>
````

**SSR 澶勭悊鏂瑰紡**锛氭瀯寤烘椂浠?Open Props 鎻愬彇鐢ㄥ埌鐨勫彉閲忓€硷紝鍐呰仈鍒扮粍浠?sheet 涓€俈ite 鐨?postcss-import 鎴栫畝鍗曠殑鎵嬪姩鎻愬彇鑴氭湰鍗冲彲銆?

### 5.4 涓庡綋鍓嶆柟妗堢殑鏄犲皠

| 褰撳墠                          | v0.20                                                   | 璇存槑                     |
| ------------------------------- | ------------------------------------------------------- | -------------------------- |
| `color-values.ts` (~40 棰滆壊)  | **鍒犻櫎**锛岀敤 Open Props `--gray-*` 绯诲垪           | 鍑忓皯缁存姢               |
| `design-tokens.ts` (~30 鍙橀噺) | **鍒犻櫎**锛岀敤 Open Props `--size-*` `--radius-*` 绛? | 鍚屼笂                     |
| `colors.ts` (璇箟鏄犲皠)       | 鍝佺墝鑹茬敤 `--brand` fallback `--indigo-6`            | 鏋佺畝瑕嗙洊               |
| `css\`...\`` (Lit)              | CSSStyleSheet + replaceSync()                           | 璇硶涓嶅悓锛屾晥鏋滅瓑浠? |
| 鍐呰仈 cssText                  | CSSStyleSheet                                           | **娑堥櫎**                 |

### 5.5 缁勪欢绀轰緥

```typescript
// less-button.ts 鈥?Open Props + DsdElement
import { DsdElement } from '@openelement/core';

const sheet = new CSSStyleSheet();
sheet.replaceSync(`
  :host { display: inline-block; }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--size-2);
    font-family: var(--font-sans);
    font-weight: var(--font-weight-5);
    border: var(--border-size-1) solid var(--gray-5);
    background: transparent;
    color: var(--gray-9);
    border-radius: var(--radius-2);
    padding: var(--size-2) var(--size-4);
    transition: color var(--ease-2), background var(--ease-2);
  }

  .btn--primary {
    background: var(--brand, var(--indigo-6));
    color: var(--gray-0);
    border-color: transparent;
  }

  .btn--primary:hover { background: var(--indigo-7); }

  .btn--sm { padding: var(--size-1) var(--size-3); font-size: var(--font-size-0); }
  .btn--lg { padding: var(--size-3) var(--size-5); font-size: var(--font-size-3); }

  .btn:disabled { opacity: 0.5; cursor: not-allowed; }
`);

export class LessButton extends DsdElement {
  static styles = sheet;
  static hydrateEvents = [
    { selector: '.btn', event: 'click', method: '_handleClick' },
  ];

  render(): string {
    const v = this.getAttribute('variant') || 'default';
    const s = this.getAttribute('size') || 'md';
    const d = this.hasAttribute('disabled');
    return `<button class="btn btn--${v} btn--${s}"
      part="control" ${d ? 'disabled' : ''}>
      <slot></slot></button>`;
  }
}
```

---

## 6. 鏋勫缓浜х墿瀵规瘮

| 鎸囨爣          | v0.19.0 (Lit)      | v0.20.0 (鍘熺敓)              | 鍙樺寲          |
| --------------- | ------------------ | ----------------------------- | --------------- |
| UI 鍖?gzip (JS) | ~12KB              | ~6KB                          | **-50%**        |
| Lit 渚濊禆      | lit (~6KB)         | 0                             | **绉婚櫎**      |
| 閫傞厤鍣ㄤ緷璧? | adapter-lit (~4KB) | 0 (DSD) + 淇濈暀 (Island)     | *_DSD 璺緞闆?_ |
| 棣栧睆 JS       | 0 KB (DSD 宸查浂)  | 0 KB                          | 涓嶅彉          |
| CSS 鎻愬彇      | Lit adapter 閬嶅巻 | 鍘熺敓 CSSStyleSheet.cssRules | 鏇村揩          |

---

## 7. 鍏变韩鐭ヨ瘑锛堣法鏂囦欢绾﹀畾锛?

### 7.1 hydrateEvents 鍗忚锛堜笉鍙橈級

```typescript
interface HydrateEventDescriptor {
  selector: string; // CSS selector within shadow root
  event: string; // DOM event name ('click', 'input', 'change')
  method: string; // instance method name (no leading __)
}
```

### 7.2 render() SSR 濂戠害

````
render(): string
  鈫?杩斿洖 Shadow DOM HTML
  鈫?鍖呭惈 <slot> 鍏冪礌锛堥€忎紶瀛愯妭鐐癸級
  鈫?鍏冪礌涓婃爣娉?part="..." 灞炴€э紙澶栭儴瀹氬埗 API锛?  鈫?涓嶄娇鐢ㄦ鏋剁壒瀹氱殑缁戝畾璇硶锛園click銆?prop銆?attr锛?```

### 7.3 CSSStyleSheet 鍏变韩瑙勫垯

- 姣忎釜缁勪欢 **涓€涓?* `static styles: CSSStyleSheet`
- Token sheet 鍙互鐙珛寮曠敤锛坕mport { tokenSheet } from './design-tokens.js'锛?- 缁勪欢 sheet 涓嶅寘鍚?token锛堢敱 DsdElement 鍩虹被鍚堝苟锛?- 璺ㄧ粍浠跺叡浜牱寮?鈫?鎻愬彇鍒扮嫭绔?sheet

---

## 8. 渚濊禆鍖呭垪琛?
| 鍖?          | 鐗堟湰 | 鐢ㄩ€?             |
| ------------ | ---- | ----------------- |
| (鏃犳柊澧炰緷璧? | 鈥?   | DsdElement 闆朵緷璧?|

闇€浠?`packages/ui/deno.json` 绉婚櫎锛?
- `lit` (DSD 缁勪欢涓嶅啀闇€瑕?
- `@openelement/adapter-lit` (DSD 缁勪欢涓嶅啀闇€瑕?

淇濈暀锛堜粎 Island锛夛細

- `lit` (less-hero-ping)
- `@openelement/adapter-lit` (SSR 绠＄嚎涓殑 Pure Island 閫傞厤)

---

## 9. 鏂囦欢鍒楄〃

### 鏂板鏂囦欢
````

packages/core/src/dsd-element.ts # DsdElement 鍩虹被 (~150 琛?
packages/core/**tests**/dsd-element.test.ts # DsdElement 娴嬭瘯

```
### 淇敼鏂囦欢
```

packages/core/src/index.ts # +export { DsdElement }
packages/core/src/render-dsd.ts # +CSSStyleSheet 鎻愬彇 (~15 琛?
packages/core/src/types.ts # 纭 HydrateEventDescriptor 婊¤冻闇€姹?packages/ui/src/design-tokens.ts # CSSResult 鈫?CSSStyleSheet
packages/ui/src\/open-card.ts # Lit 鈫?DsdElement
packages/ui/src\/open-callout.ts # Lit 鈫?DsdElement
packages/ui/src\/open-step-card.ts # Lit 鈫?DsdElement
packages/ui/src\/open-button.ts # Lit 鈫?DsdElement
packages/ui/src\/open-input.ts # Lit 鈫?DsdElement
packages/ui/src\/open-code-block.ts # Lit 鈫?DsdElement
packages/ui/src\/open-theme-toggle.ts # Lit 鈫?DsdElement
packages/ui/src\/open-dialog.ts # Lit 鈫?DsdElement
packages/ui/src\/open-layout.ts # Lit 鈫?DsdElement (鍒嗗眰)
packages/ui/src/less-search.ts # Lit 鈫?DsdElement
packages/ui/deno.json # 绉婚櫎 lit + adapter-lit 渚濊禆
packages/adapter-lit/README.md # 鏍囨敞"浠呮湇鍔?Pure Island"

```
### 涓嶅彉鏂囦欢
```

packages/ui/src\/open-hero-ping.ts # 淇濈暀 Lit (绾?Island)
packages/adapter-lit/src/dsd-hydration.ts # 淇濈暀 (DsdLitElement 鍚戝悗鍏煎)
packages/adapter-lit/src/ssr.ts # 淇濈暀 (Island SSR)
packages/core/src/render-nested.ts # 涓嶅彉
packages/core/src/adapter-registry.ts # 涓嶅彉

````
---

## 10. 浠诲姟鍒楄〃锛堟湁搴忥紝鍚緷璧栵級

| #  | 浠诲姟                          | 鏂囦欢                                    | 渚濊禆       | 棰勮宸ユ椂 |
| -- | ----------------------------- | --------------------------------------- | ---------- | -------- |
| 1  | 鍒涘缓 DsdElement 鍩虹被          | `packages/core/src/dsd-element.ts`      | 鈥?         | 0.5d     |
| 2  | 鏇存柊 core 瀵煎嚭                | `packages/core/src/index.ts`            | 1          | 0.1d     |
| 3  | CSSStyleSheet 鎻愬彇閫傞厤        | `packages/core/src/render-dsd.ts`       | 1          | 0.5d     |
| 4  | Token 杩佺Щ涓?CSSStyleSheet    | `packages/ui/src/design-tokens.ts`      | 1          | 0.3d     |
| 5  | 杩佺Щ less-card (P0-A)         | `packages/ui/src\/open-card.ts`         | 1, 3, 4    | 0.3d     |
| 6  | 杩佺Щ less-callout (P0-B)      | `packages/ui/src\/open-callout.ts`      | 5          | 0.2d     |
| 7  | 杩佺Щ less-step-card (P0-C)    | `packages/ui/src\/open-step-card.ts`    | 5          | 0.2d     |
| 8  | 杩佺Щ less-button (P0-D)       | `packages/ui/src\/open-button.ts`       | 1, 3, 4    | 0.5d     |
| 9  | SSR 鍏ㄩ摼璺獙璇?               | `www/` 鏋勫缓                             | 5, 6, 7, 8 | 0.5d     |
| 10 | 杩佺Щ less-input (P1-A)        | `packages/ui/src\/open-input.ts`        | 9          | 0.3d     |
| 11 | 杩佺Щ less-code-block (P1-B)   | `packages/ui/src\/open-code-block.ts`   | 9          | 0.3d     |
| 12 | 杩佺Щ less-theme-toggle (P1-C) | `packages/ui/src\/open-theme-toggle.ts` | 9          | 0.3d     |
| 13 | 杩佺Щ less-dialog (P1-D)       | `packages/ui/src\/open-dialog.ts`       | 9          | 0.5d     |
| 14 | 杩佺Щ less-layout (P1-E)       | `packages/ui/src\/open-layout.ts`       | 9          | 1.5d     |
| 15 | 杩佺Щ less-search (P2-A)       | `packages/ui/src/less-search.ts`        | 9          | 1d       |
| 16 | 鏇存柊 deno.json 渚濊禆           | `packages/ui/deno.json`                 | 14, 15     | 0.1d     |
| 17 | 鍏ㄩ噺鍥炲綊娴嬭瘯                  | 鎵€鏈夌粍浠?                               | 16         | 0.5d     |
| 18 | Bundle 浣撶Н鎶ュ憡               | `www/` 鏋勫缓浜х墿                         | 17         | 0.3d     |

---

## 11. 璋冪敤娴佺▼鍥撅紙鏃跺簭鍥撅級

```mermaid
sequenceDiagram
    participant SSG as SSG Build
    participant Core as @openelement/core
    participant UI as @openelement/ui
    participant Adapter as @openelement/adapter-lit
    participant Browser as Browser

    Note over SSG,Browser: 鈹€鈹€ Ocean (DSD Component) 鈹€鈹€

    SSG->>Core: renderDsd('less-button', LessButton, props)
    Core->>Core: instantiate LessButton
    Core->>Core: injectProps(props)
    Core->>UI: instance.render()
    UI-->>Core: "<button class='btn' part='control'><slot></slot></button>"
    Core->>Core: extract CSSStyleSheet.cssRules
    Core->>Core: wrapDsdOutput()
    Core-->>SSG: <open-button><template shadowrootmode="open">...</template></open-button>

    SSG->>Browser: HTML response
    Browser->>Browser: Parse DSD 鈫?auto-attach Shadow DOM
    Browser->>Browser: customElements.define('less-button', LessButton)
    Browser->>UI: createRenderRoot() 鈫?detect DSD 鈫?_dsdHydrated=true
    Browser->>UI: connectedCallback() 鈫?hydrateEvents()
    Note over Browser: 鉁?Interactive. Zero framework overhead.

    Note over SSG,Browser: 鈹€鈹€ Island (Pure Island) 鈹€鈹€

    SSG->>Core: renderDsd('less-hero-ping', LessHeroPing, {})
    Core->>Core: instance.render() 鈫?TemplateResult
    Core->>Adapter: adapter.render(TemplateResult)
    Adapter->>Adapter: interpolate() 鈫?HTML string
    Adapter-->>Core: "<span>...</span>"
    Core-->>SSG: <open-hero-ping><template shadowrootmode="open">...</template></open-hero-ping>

    SSG->>Browser: HTML response
    Browser->>Browser: Parse DSD 鈫?Shadow DOM
    Browser->>Browser: LitElement upgrade 鈫?render()
    Note over Browser: 鉁?Interactive via Lit reactivity.
````

---

## 12. 椋庨櫓涓庣紦瑙?

| 椋庨櫓                                    | 姒傜巼 | 褰卞搷 | 缂撹В                                        |
| ----------------------------------------- | ------ | ------ | -------------------------------------------- |
| `less-layout` 杩佺Щ寮曞叆鍥炲綊           | 涓?    | 楂?    | 鍒嗗眰杩佺Щ + 姣忔 SSR 杈撳嚭 diff          |
| CSSStyleSheet.cssRules 璺ㄥ煙璇诲彇澶辫触 | 浣?    | 浣?    | 鍐呰仈 sheet 涓嶄細鏈夎法鍩熼棶棰?           |
| `@property` Safari 涓嶆敮鎸?              | 涓?    | 浣?    | 闄嶇骇涓烘櫘閫?CSS 鍙橀噺                    |
| 浜嬩欢缁戝畾鍦?CSR 璺緞閲嶅             | 浣?    | 涓?    | DsdElement 宸查€氳繃 `_dsdHydrated` 鍖哄垎   |
| hydrateEvents 閫夋嫨鍣ㄥ尮閰嶅け璐?       | 浣?    | 涓?    | 淇濇寔涓庡綋鍓?Lit 缁勪欢鐩稿悓鐨?DOM 缁撴瀯 |

---

## 13. 寰呮槑纭簨椤?

1. **@property 娉ㄥ唽鏃舵満**锛氬湪 token sheet 涓０鏄庤繕鏄湪鐙珛 `<style>` 涓紵鈫?寤鸿鍦ㄥ叏灞€ `<style>` 涓敞鍐岋紙`:root` 绾у埆锛夛紝token sheet 鍙畾涔夊彉閲忓€?2. **CSSStyleSheet 鍚堝苟绛栫暐**锛歵oken sheet + 缁勪欢 sheet 鑷姩鍚堝苟 or 鎵嬪姩鍚堝苟锛熲啋 DsdElement 鍩虹被鑷姩鍚堝苟
2. **`@openelement/adapter-lit` 淇濈暀绛栫暐**锛歷0.20 淇濈暀鍏煎灞傦紝v0.21 搴熷純 DSD 璺緞锛堜粎淇濈暀 Island SSR锛夆啋 纭

---

## 14. 鍏抽敭璁捐鍐崇瓥璁板綍

| 鍐崇瓥                | 鏂规                                 | 鍘熷洜                                  |
| --------------------- | ------------------------------------- | --------------------------------------- |
| DsdElement 浣嶇疆     | `@openelement/core`                   | 闆朵緷璧栵紝涓?renderDsd 鍚屽寘         |
| render() 杩斿洖绫诲瀷 | `string`                              | 涓?renderDsd 鐜版湁璺緞涓€鑷?          |
| 鏍峰紡鏂规           | CSSStyleSheet (adoptedStyleSheets)    | 娴忚鍣ㄥ師鐢燂紝涓€娆¤В鏋愬叏灞€鍏变韩 |
| hydrateEvents 淇濈暀  | 鉁?                                   | 鎴愮啛妯″紡锛屾棤闇€鏀瑰彉              |
| :state() 鏀寔        | ElementInternals                      | 绾爣鍑嗭紝Lit 涔熷湪鐢?                |
| `updated()` 鏇夸唬    | attributeChangedCallback + _syncDOM() | 鍘熺敓 API锛屾牱鏉夸唬鐮佸彲鎺ュ彈      |
| `less-hero-ping`      | 淇濈暀 Lit                            | 鍞竴绾?Island锛岄渶瑕?reactivity       |
