{
  "name": "@kiss/docs",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "cd ../packages/kiss-ui && npx vite build --config vite.config.build.ts",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@kissjs/ui": "file:../packages/kiss-ui"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
