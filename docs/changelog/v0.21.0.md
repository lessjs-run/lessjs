# v0.21.0 - 2026-05-23

## Summary

v0.21.0 ships Reactive DSD: native `html` templates, safe interpolation,
Signal-driven `DsdElement` updates, and Response-compatible streaming DSD.

## Changes

### Reactive DSD Runtime

- `DsdElement.render()` now supports `string | TemplateResult`.
- Existing static `render(): string` components keep the old CSR and DSD behavior.
- Components returning `html` templates render in CSR fallback and SSR `renderDSD()`.
- SSR `renderDSD()` now preserves internal runtime markers for `TemplateResult`
  event/property bindings so Declarative Shadow DOM upgrade can attach behavior.
- Template event bindings such as `@click=${handler}` attach after CSR render and
  DSD upgrade without serializing functions into HTML.
- Template property bindings such as `.value=${value}` are applied on the client
  without becoming HTML attributes.
- Signal-like values with `.value` and `.subscribe()` are collected from templates.
- Signal writes schedule microtask-batched component-local rerenders.
- Signal subscriptions and template event listeners are cleaned up on disconnect.

### Safe Templates

- Added `packages/core/src/template.ts`.
- Added public `html`, `unsafeHTML`, `isTemplateResult`, `isUnsafeHTML`,
  `isSignalLike`, and `renderTemplateToString` exports from `@lessjs/core`.
- Text interpolation escapes HTML by default.
- Attribute interpolation escapes quote and tag breakout vectors.
- URL-sensitive attributes such as `href` and `src` reject dangerous protocols.
- `unsafeHTML()` is the explicit trust boundary for raw HTML insertion.
- Nested templates and arrays are rendered recursively.
- Event handlers and property values are excluded from SSR HTML output.

### Streaming DSD

- Added `renderDSDStream()` to `@lessjs/core`.
- Added `createRenderDSDStreamMetrics()` and stream option/types for chunk and
  error observation.
- Stream output emits shell first, component chunks next, and footer last.
- Stream chunks are encoded as `Uint8Array` so `new Response(stream)` works in
  Deno, Workers-style runtimes, and browser Web Streams.
- `onChunk` still exposes the rendered HTML string and structured `RenderOutput`
  for diagnostics.

### UI And Website Adoption

- `less-theme-toggle` now uses Reactive DSD directly: `html` templates,
  component-local Signals, safe `unsafeHTML()` icon insertion, and template
  `@click` binding.
- Removed the theme toggle's old manual `hydrateEvents` and imperative DOM
  update path in favor of the v0.21 runtime contract.
- Added `@lessjs/signals/framework` as the side-effect-light public subpath for
  framework-level Signal primitives.

### Architecture Boundary

- No DOM diff API was added.
- No virtual DOM, compiler, JSX transform, Vite, Hono, Node, or npm runtime
  dependency was introduced into `@lessjs/core`.
- Complex UI remains an Island responsibility; Reactive DSD is for small
  DSD-native Web Component interactions.

### `hydrateEvents` Removal

- Removed `static hydrateEvents` from `DsdElement` core class. Components MUST use
  `html` tagged template `@click` / `@keydown` bindings instead.
- Removed `hydrateEvents` from `HydrationHint`, `LessElementExtensions`, `DsdComponent`
  types, and the `render-dsd.ts` hydration pipeline.
- Removed `_hydrateEvents()` method and `_hydrateAbortController` field (40+ lines).
- Migrated all internal islands (`less-term`, `counter-island`, `less-toc`,
  `less-layout`) and `create` scaffold templates to `html` + `@click`.
- Public docs example code updated to show `html` template pattern exclusively.

### Package Boundary Split (SOP-007)

- Extracted `@lessjs/style-sheet` (CSSStyleSheet wrapper) from `@lessjs/core`.
- Extracted `@lessjs/compat-check` (CEM classification, tag validation) from core.
- Extracted `@lessjs/cem` (CEM parser/reader) from core with `parseCem`, `readCemFile`,
  `classifyCemManifest`, `extractLessDeclarations`, `findModulePathForTag`.
- Core re-exports preserved via subpath exports (`@lessjs/core/compatibility`,
  `@lessjs/core/cem-parser`) for backward compatibility.
- All new packages have independent `deno.json` and publish paths.

### ReactiveHost Protocol (SOP-008)

- Added `ReactiveHost` interface with explicit `subscribeTo()` and
  `requestReactiveUpdate()` contract, replacing Duck Typing `isSignalLike()` checks.
- `DsdElement` now declares `implements ReactiveHost`.
- `_subscribeTemplateSignals()` uses `this.subscribeTo(signal)` via protocol dispatch
  instead of direct `signal.subscribe()`.
- `requestReactiveUpdate()` is public, enabling framework-level scheduling control.
- Exported `ReactiveHost` and `Unsubscribe` from `@lessjs/core`.

### SOP And Strategy Docs

- Rewrote `docs/sop/v0.21.0` as five detailed SOPs:
  SOP-001 `DsdElement + Signals`, SOP-002 `Safe Templates`,
  SOP-003 `Streaming DSD`, SOP-004 `Integration Depth + DX`,
  SOP-005 `Verification + Release Gate`.
- Removed the old optional DOM diff SOP from v0.21 scope.
- SOP-004 delivered: fine-grained `data-b` markers, `_patchBindings` reactive
  DOM patching (no full innerHTML replacement after initial render),
  single-import DX (`import { DsdElement, html, signal } from '@lessjs/core'`).
- `@lessjs/core` now re-exports `signal`, `computed`, `effect` from
  `@lessjs/signals/framework`.
- Rewrote `docs/sop/v0.22.0` around Edge Full-Stack.
- Updated roadmap/status/README strategy text.

## Breaking Changes

- **`static hydrateEvents` removed from `DsdElement`.** All components MUST use
  `html` tagged template `@click` / `@keydown` bindings. The `_hydrateEvents()`
  method and `HydrateEventDescriptor` type are removed from the core runtime.
  Migration: replace `static hydrateEvents = [...]` + `render(): string` with
  `override render() { return html\`<button @click=\${handler}>...\`; }`.
- `renderDSDStream()` returns a byte stream (`ReadableStream<Uint8Array>`) rather
  than a string stream so it can be passed directly to `new Response(stream)`.
  Consumers that need text chunks should use `onChunk`.
- UI tests and website components now assert the Signal-based theme state rather
  than the removed `_isLight` implementation detail.

## Known Issues

- Reactive DSD currently uses microtask-batched component-local rerendering for
  Signal changes; it intentionally does not include a general DOM diff engine.
- Fine-grained keyed list reconciliation, focus-preserving subtree diffing, and
  complex stateful surfaces remain Island responsibilities.

## Verification

- `deno fmt --check packages/core/src/template.ts packages/core/src/dsd-element.ts packages/core/src/render-dsd.ts packages/core/src/index.ts packages/core/__tests__/template.test.ts`
- `deno lint packages/core/src/template.ts packages/core/src/dsd-element.ts packages/core/src/render-dsd.ts packages/core/__tests__/template.test.ts packages/core/__tests__/reactive-dsd.test.ts`
- `deno task typecheck`
- `deno test --allow-read --allow-write --allow-env --allow-net --allow-run packages/core/__tests__/reactive-dsd.test.ts packages/core/__tests__/template.test.ts packages/core/__tests__/render-dsd.test.ts`
- `deno task fmt:check`
- `deno task lint`
- `deno task docs:check-strategy`
- `deno audit`
- `deno test --allow-read --allow-write --allow-env --allow-net --allow-run packages/core/__tests__/template.test.ts packages/core/__tests__/reactive-dsd.test.ts packages/ui/__tests__/components.test.ts www/__tests__/islands-reactivity.test.ts` -
  87 passed
- `deno test --allow-read --allow-write --allow-env --allow-net --allow-run packages/create/__tests__/cli.test.ts` -
  14 passed
- `deno task test` - 760 passed

### Code Quality Hardening (2026-05-23 Final)

- Added `try/catch` around `_patchBindings()` to prevent silent failures on
  render errors (SOP-009 P3.1).
- Renamed `data-b` markers to `data-less-b` to prevent collision with user
  templates (SOP-009 P3.2).
- Extracted `renderDSDStream()` and stream types to `render-dsd-stream.ts`
  (SOP-009 P2.2).
- Physically copied `compatibility.ts` and `cem-parser.ts` implementations to
  `@lessjs/compat-check` and `@lessjs/cem` packages for independent builds
  (SOP-009 P2.1).

### Test Expansion

- `reactive-dsd.test.ts`: 13 tests (was 1) — covering text binding, event
  handler, fine-grained patching, batching, cleanup, nested templates, computed
  signals, conditional tracking, multiple signals, SSR safety.
- `template.test.ts`: 15 tests (was 7) — covering XSS vectors, attribute
  breakout, URL protocol attacks, boolean binding, property no-serialize,
  event no-serialize, `unsafeHTML`, nested escape.
- Added `streaming-dsd.test.ts`: 7 tests for `renderDSDStream()` — stream type,
  shell ordering, footer ordering, priority, failure fallback, TemplateResult
  escaping, metrics.
- **Known limitation**: `computed()` signal re-patching depends on the inner
  effect chain in `@lessjs/signals/framework`. Re-subscription hardening is
  tracked for v0.22.0.

### Signal Migration

- `less-search.ts`: Migrated `_open`, `_query`, `_results` to `#open`, `#query`,
  `#results` signals (SOP-004 Step 5). Overlay remains imperative
  (document.body).
- `less-toc.ts`: Migrated `_headings`, `_activeId` to signals. `render()` uses
  `html` tagged template with `@click`. Removed `_updateDOM()`.
- `less-term.ts`: Migrated `_cmdHistory`, `_historyIdx` to signals. Terminal IO
  remains imperative.

### Documentation

- Created `docs/guide/migrating-from-lit.md` — Lit → DsdElement+Signals
  migration guide with comparison table, counter examples, and checklist.
- Created `docs/sop/v0.21.0/SOP-007-core-package-split.md` — documents the
  compat-check/cem/style-sheet extraction.
- Created `docs/sop/v0.21.0/SOP-008-reactivehost-protocol.md` — documents the
  ReactiveHost protocol replacing Duck Typing.
- Created `docs/sop/v0.21.0/SOP-009-closure-and-remediation.md` — 11-action-item
  closure plan from comprehensive review; all P0/P1 items completed.
- Updated `docs/status/STATUS.md`: 16 packages at v0.21.0, 9 SOPs.
- Updated `docs/sop/v0.21.0/README.md`: release order updated with completion
  status, SOP-009 added.

## Final Verification (2026-05-23)

```sh
deno fmt          # 488 files formatted
deno lint         # 277 files checked, 0 problems
deno task typecheck  # clean
deno task test    # 787 passed, 0 failed
deno task build   # clean
```

- `deno task build` - clean build
- `deno task dsd:check-report` - passed with known third-party SSR boundary errors
- `deno task test:e2e` - 92 passed

### Architect Review Remediation (2026-05-24)

Triggered by cross-review (高见远, 8.2/10). Five code quality fixes:

1. **Eliminated code duplication**: Deleted duplicate `compatibility.ts` (492 lines)
   and `cem-parser.ts` (458 lines) from `@lessjs/compat-check` and `@lessjs/cem`.
   Canonical implementations live in `@lessjs/core`; standalone packages import
   via subpath exports. Net reduction: ~950 lines of duplicated code.
2. **Added `./types` subpath export** to `@lessjs/core` (`deno.json`). Also
   added `./render-dsd-stream` for the extracted streaming module.
3. **Deprecated `batch()`** in `@lessjs/signals`. Now emits a `console.warn`
   explaining that DsdElement's `_scheduleReactiveUpdate()` already provides
   microtask-level batching. Will be removed in v1.0.0.
4. **Extracted `DANGEROUS_KEYS`** from `island.ts` to new `security.ts`.
   Eliminated false coupling where SSR module (`render-instantiate.ts`) imported
   from client module (`island.ts`).
5. **Simplified `connectedCallback`** in `DsdElement`: extracted DSD/CSR dispatch
   into `_hydrateOrRender()` private method, reducing if-else nesting.

## Final Verification (2026-05-24, SOP-010 complete)

```sh
deno fmt          # 488 files formatted
deno lint         # 276 files checked, 0 problems
deno task typecheck  # clean
deno task test    # 787 passed, 0 failed
deno task build   # clean
deno task test:e2e   # 92 passed
```
