# LessJS v0.24.0 — DX Leap

**2026-05-28** | v0.24.0 | ADR-0051, ADR-0052, ADR-0053 | SOP-009 ~ SOP-015

v0.24.0 is the DX Leap release. It transforms LessJS from "architecture 8.5,
DX 5" to "architecture 8.5, DX 8". Three ADRs drive the changes.

## What Shipped

### SOP-009: html Template Strengthening (ADR-0051)

Five template helpers + template caching added to `packages/core/src/template.ts`
(from ~309 → ~500 lines):

| Helper                          | Purpose                                                                |
| ------------------------------- | ---------------------------------------------------------------------- |
| `classMap(obj)`                 | Dynamic CSS classes (`{ active: true, disabled: false }` → `"active"`) |
| `when(cond, t, f?)`             | Conditional rendering (eager + lazy)                                   |
| `choose(key, cases, fallback?)` | Multi-way conditionals                                                 |
| `repeat(items, keyFn, fn)`      | Keyed iteration with identity tracking                                 |
| `ref(callback)`                 | DOM reference directive (replaces `querySelector`)                     |

Template caching via `WeakMap<TemplateStringsArray, ParsedTemplate>` avoids
re-parsing static parts on re-render.

Type narrowing: `AttrValue`, `ContentValue`, `EventValue` replace the broad
`TemplateValue` union.

All helpers re-exported through `@lessjs/runtime`.

### SOP-010: Signal-DOM Deep Integration (ADR-0052)

`@prop()` decorator reduces property declaration from 10+ lines across 4
locations to **1 line:**

```ts
count = 0;
disabled = false;
ariaLabel = '';
```

Features:

- Signal-backed accessors (via internal `PropSignal`)
- Microtask-batched DOM updates (via `ReactiveHost`)
- Attribute↔property sync with type conversion (String/Number/Boolean)
- `reflect` option writes property changes back to HTML attribute
- Auto `observedAttributes` registration
- Lifecycle cleanup via `disposeProps()` in `disconnectedCallback()`

New file: `packages/core/src/prop.ts`

### SOP-011: Unified Error Handling (ADR-0053)

Four-layer error architecture:

| Layer | Component       | Location                                                                              |
| ----- | --------------- | ------------------------------------------------------------------------------------- |
| 1     | Typed hierarchy | `errors.ts` — `LessError` → `RenderError`/`NavigationError`/`BuildError` + subclasses |
| 2     | ErrorBoundary   | `error-boundary.ts` — catch child render failures, display fallback UI                |
| 3     | Propagation     | `SsrErrorContext` — SSR error accumulation + merge                                    |
| 4     | Telemetry       | `setErrorTelemetryHook()` / `reportError()` — global observer                         |

All errors carry: `code` (ErrorCode enum), `severity`, `phase`, `recoverable`, `cause`.

### SOP-012: Test Coverage Supplement

New tests:

- `packages/style-sheet/__tests__/style-sheet.test.ts` (5 cases)
- `packages/core/__tests__/template-helpers.test.ts` (5 cases: classMap/when/choose/repeat/ref)
- `packages/core/__tests__/errors.test.ts` (updated to new API)

### SOP-013: Type Safety Hardening

`any` count in core/runtime/signals/style-sheet: **1** (from 50+).
Achieved through existing incremental hardening in v0.22-v0.23.

### SOP-014: Package README Updates

- `packages/core/README.md`: Added @prop, html helpers, ErrorBoundary usage
- `packages/runtime/README.md`: Updated with v0.24 import examples

### SOP-015: Validation Gate

```sh
deno task fmt:check  ✅
deno task test       ✅ (927+ passed)
deno task typecheck  ✅
```

## CI Fixes (v0.23.x Release Closure)

Multiple rounds of fixes applied during v0.23.0 → v0.23.6:

| ADR      | Description                                                              |
| -------- | ------------------------------------------------------------------------ |
| ADR-0054 | AST-based subpath export resolution (replaces FALLBACK_REGEX_MAP)        |
| ADR-0056 | Consumer import map + AST (correct external SSR dependency architecture) |

Consumer template now includes 9 imports (v0.23.0 had 5):
`alien-signals`, `entities`, `hono`, `parse5` added as declared SSR dependencies.

## Package Versions

All 17 packages at `0.24.0`.

| Package                 | Version |
| ----------------------- | ------- |
| @lessjs/adapter-lit     | 0.24.0  |
| @lessjs/adapter-react   | 0.24.0  |
| @lessjs/adapter-vanilla | 0.24.0  |
| @lessjs/adapter-vite    | 0.24.0  |
| @lessjs/app             | 0.24.0  |
| @lessjs/cem             | 0.24.0  |
| @lessjs/compat-check    | 0.24.0  |
| @lessjs/content         | 0.24.0  |
| @lessjs/core            | 0.24.0  |
| @lessjs/create          | 0.24.0  |
| @lessjs/hub             | 0.24.0  |
| @lessjs/i18n            | 0.24.0  |
| @lessjs/protocols       | 0.24.0  |
| @lessjs/rpc             | 0.24.0  |
| @lessjs/runtime         | 0.24.0  |
| @lessjs/signals         | 0.24.0  |
| @lessjs/style-sheet     | 0.24.0  |
| @lessjs/ui              | 0.24.0  |
