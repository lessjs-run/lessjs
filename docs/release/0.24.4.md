# v0.24.4: API Cleanup Line Endpoint

> **Released**: 2026-05-29
> **Previous**: v0.24.3

## Summary

v0.24.4 is the **cleanup line endpoint** — the final release before v0.25.0
ecosystem hardening. API naming is unified to Web Platform conventions,
`less()` is removed from the public API, SSG DSD output is restored, and
internal cleanup is complete. No backward compatibility shipped.

## Breaking Changes

**`less()` removed from `@openelement/adapter-vite`.** Use `lessPipeline()` instead:

```diff
- import { less } from '@openelement/adapter-vite';
+ import { lessPipeline } from '@openelement/adapter-vite';

- less({ routesDir: 'app/routes', island: { upgradeStrategy: 'idle' } })
+ lessPipeline({ routes: { dir: 'app/routes' }, island: { dir: 'app/islands', upgradeStrategy: 'idle' } })
```

**API renames** (no aliases):

```diff
- import { island, lessBind, renderDSD, renderToDOM, getSSRProps } from '@openelement/runtime';
+ import { defineIsland, bindEvents, renderDsd, renderToDom, getSsrProps } from '@openelement/runtime';
```

## What's New

### lessPipeline() Declarative API

- `lessPipeline(config: PipelineConfig): Plugin[]` — single public build entry
- Config shape: `{ routes, i18n, output, island, pwa, viewTransition, headExtras }`
- Maps to `FrameworkOptions` internally — all existing build code reused

### SSG DSD Output Restored

- **Root cause**: `import()`-based tagName detection silently failed for routes
  with `virtual:less-nav` dependencies
- **Fix**: Regex-based source scan on `export const tagName` — zero module execution
- DSD markers in generated pages: 3 (polyfill only) → 5 (real `<template shadowrootmode>`)

### SSG Smoke Test Hardening

- Removed `dist/` cache skip — test always does fresh build
- Prevents stale output from masking regressions across CI runs

### customElements Duplicate Registration Fix

- Added `!customElements.get(tagName)` guard to 4 island components
- Prevents `Failed to execute 'define'` errors during SSR bundle loading

### Internal Cleanup

| Before                 | After                                       |
| ---------------------- | ------------------------------------------- |
| `less()` public API    | Removed — `lessPipeline()` only             |
| `_dsdHydrated` field   | Removed — unified `_renderOrHydrate()` path |
| `__lessBindDone` guard | `__bindEventsDone`                          |
| tagName detection      | Regex source scan (was `import()`)          |

## Verification

| Gate                    | Result                    |
| ----------------------- | ------------------------- |
| `deno fmt`              | ✅ 689 files              |
| `deno lint`             | ✅ 331 files              |
| `deno task typecheck`   | ✅                        |
| `deno task graph:check` | ✅ 18 packages, 0 cycles  |
| `deno test`             | ✅ 943 passed, 0 failed   |
| SSG smoke               | ✅ 4/4 steps, DSD present |

## File Changes

| Package                     | Key Changes                                                         |
| --------------------------- | ------------------------------------------------------------------- |
| `@openelement/adapter-vite` | `less()` removed, `lessPipeline()` default export, tagName fix      |
| `@openelement/core`         | `_dsdHydrated` removed, `Function` type suppress, unused lint clean |
| `@openelement/app`          | Internal import from `less-plugin` subpath                          |
| `@openelement/runtime`      | API renames applied                                                 |
| `www/`                      | 4 island guards, JSX lint fixes, API ref update, PWA guide update   |
| `docs/`                     | CHANGELOG, release notes, changelog v0.24.4                         |
